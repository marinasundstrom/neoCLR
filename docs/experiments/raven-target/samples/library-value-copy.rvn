import System.Option.*
import System.*
import System.Console.*

func Show(value: Option<int>) {
    if value is .Some(let found) {
        WriteLine(found)
    }
}

func Main() {
    var original: Option<int> = Some(42)
    let copy = original
    original = Some(7)
    Show(copy)
    Show(original)
}
