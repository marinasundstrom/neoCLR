import System.*
import System.Console.*

func Make() -> int[] {
    return [1, 2]
}

func Change(values: int[]) {
    values[0] = 42
}

func Main() {
    let values = Make()
    let alias = values
    Change(alias)
    WriteLine(values[0])
    WriteLine(values.Length)
}
