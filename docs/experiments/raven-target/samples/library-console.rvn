import System.*
import System.Console.*

func Read() -> Result<Option<byte>, IO.ConsoleReadError> {
    let value = ReadByte()?
    return Result<Option<byte>, IO.ConsoleReadError>.Ok(value)
}

func Show() {
    let result = Read()
    if result is .Ok(let ok) {
        if ok is .Some(let some) {
            WriteLine((int)some)
        } else {
            WriteLine("End of input")
        }
    } else if result is .Error(let error) {
        WriteLine(error.ToString())
    }
}

func Main() {
    Show(); Show(); Show()
}
