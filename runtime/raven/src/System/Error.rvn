namespace System

import System.Runtime.CompilerServices.RuntimeServices

// The runtime owns the opaque message payload.
public struct Error {
    static func FromMessage(message: string) -> Error {
        return RuntimeServices.ErrorFromMessage(message)
    }

    val Message: string => RuntimeServices.ErrorMessage(self)

    func ToString() -> string {
        return Message
    }
}
