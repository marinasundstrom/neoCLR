namespace System.Text

public union Utf8SliceError {
    case OutOfRange
    case InvalidBoundary

    override func ToString() -> string {
        return match self {
            OutOfRange => "OutOfRange"
            InvalidBoundary => "InvalidBoundary"
            _ => "Empty"
        }
    }
}
