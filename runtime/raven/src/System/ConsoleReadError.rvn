namespace System

public union ConsoleReadError {
    case Unavailable
    case ReadFailed

    override func ToString() -> string {
        return match self {
            Unavailable => "Unavailable"
            ReadFailed => "ReadFailed"
            _ => "Empty"
        }
    }
}
