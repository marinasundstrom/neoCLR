namespace System

public union Int32ParseError {
    case InvalidFormat
    case Overflow

    override func ToString() -> string {
        return match self {
            InvalidFormat => "InvalidFormat"
            Overflow => "Overflow"
            _ => "Empty"
        }
    }
}
