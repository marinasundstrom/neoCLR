namespace System.Linq

public union SingleError {
    case Empty
    case Multiple

    override func ToString() -> string {
        return match self {
            Empty => "Empty"
            Multiple => "Multiple"
            _ => "Empty"
        }
    }
}
